[package]
name = "alcedo-dev"
version = "0.1.0"
edition = "2021"

[lib]
name = "alcedo_dev"
path = "src/lib.rs"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
tokio = { version = "1", features = ["sync", "rt", "time"] }